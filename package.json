{
  "name": "monopoly-money",
  "private": true,
  "scripts": {
    "lerna": "lerna",
    "bootstrap": "lerna bootstrap --hoist",
    "start": "lerna run --scope server start",
    "build": "cd packages/server && npm run build && cd ../client && npm run build"
  },
  "devDependencies": {
    "lerna": "^3.20.2"
  }
}
